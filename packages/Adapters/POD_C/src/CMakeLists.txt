SET(HEADERS "")
SET(SOURCES "")

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

APPEND_SET(HEADERS
  DTK_C_API.h
  ) 

APPEND_SET(SOURCES
  DTK_C_API.cpp
  ) 

TRIBITS_ADD_LIBRARY(
  dtk_c_api
  HEADERS ${HEADERS}
  SOURCES ${SOURCES}
  )
