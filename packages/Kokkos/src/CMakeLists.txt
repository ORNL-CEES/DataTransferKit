
SET(HEADERS "")
SET(SOURCES "")

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
APPEND_SET(HEADERS
  DTK_PointCloud.hpp
  )

APPEND_SET(SOURCES
  libdummy.cpp
  )

TRIBITS_ADD_LIBRARY(
  dtk_kokkos
  HEADERS ${HEADERS}
  SOURCES ${SOURCES}
  )
