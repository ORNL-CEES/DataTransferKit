IF( HAVE_DATATRANSFERKITUTILS_BOOSTORG )
  GLOBAL_SET( HAVE_DTK_BOOST TRUE )
ENDIF()

SET(HEADERS "")
SET(SOURCES "")

INCLUDE_DIRECTORIES(${${PACKAGE_NAME}_BINARY_DIR})

APPEND_SET(HEADERS
   ${${PACKAGE_NAME}_BINARY_DIR}/${PACKAGE_NAME}_config.hpp
  )

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

APPEND_SET(HEADERS
  DTK_ConfigDefs.hpp
  DTK_Core.hpp
  DTK_DBC.hpp
  DTK_KokkosHelpers.hpp
  DTK_SanitizerMacros.hpp
  DTK_Types.h
  DTK_Version.hpp
  )

APPEND_SET(SOURCES
  DTK_Core.cpp
  DTK_DBC.cpp
  )

TRIBITS_ADD_LIBRARY(
  dtk_utils
  HEADERS ${HEADERS}
  SOURCES ${SOURCES}
  )
